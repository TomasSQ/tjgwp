<?xml version="1.0" encoding="UTF-8"?>
<engine>
	<dot>
		<div class="profile-header clearfix" style="{{?it.user.backgroundImage}}background: url('{{=it.user.backgroundImage}}');{{?}}">
			<img class="profile-pic margin-left"/>
			<div class="profile-info margin-left">
				<h1 class="title">{{=it.user.nickname}}</h1>
				{{? it.user.social}}
				<div class="social">
					{{~it.user.social :social}}
						<a href="{{=social.link}}" class="social-{{=social.name}}"></a>
					{{~}}
				</div>
				{{?}}
				<ul class="production italic">
					<li class="texts"><a href="#texts/{{=it.user.id}}">{{=it.user.howManyTexts ? it.user.howManyTexts + ' ' + i18n('texts') : i18n('no-texts')}}</a></li>
					<li class="awards"><a href="#awards/{{=it.user.id}}">{{=it.user.howManyAwards ? it.user.howManyAwards + ' ' + i18n('awards') : i18n('no-awards')}}</a></li>
					<li class="followers"><a href="#followers/{{=it.user.id}}">{{=it.user.howManyFollowers ? it.user.howManyFollowers + ' ' + i18n('followers') : i18n('no-followers')}}</a></li>
					<li class="following"><a href="#following/{{=it.user.id}}">{{=it.user.howManyFollowing ? it.user.howManyFollowing + ' ' + i18n('following') : i18n('no-following')}}</a></li>
				</ul>
			</div>
			<a href="#follow/{{=it.user.id}}" class="follow-button title book-icon x-icon">{{=i18n('follow')}}</a>
		</div>
		<div class="sidebar"></div>
		<div class="profile-content"></div>
	</dot>
	<script>
		$('.profile-header img').attr('src', context.user.profileImage ? context.user.profileImage : 'img/profile.png');
	</script>
</engine>